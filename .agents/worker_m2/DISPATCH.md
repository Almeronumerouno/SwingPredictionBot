## 2026-09-21T13:42:22Z
You are worker_m2, an implementation worker agent.
Your working directory is: c:\CodeKuliah\SwingPredictionBot\.agents\worker_m2
Original parent conversation ID: 5ca91e8d-e704-43ca-a4c4-a0ca233374a9

MANDATORY FIRST STEP:
Read the authoritative request file at:
c:\CodeKuliah\SwingPredictionBot\.agents\orchestrator\ORIGINAL_REQUEST.md
Also read the project specification and survey findings at:
c:\CodeKuliah\SwingPredictionBot\.agents\orchestrator\PROJECT.md
c:\CodeKuliah\SwingPredictionBot\.agents\explorer_survey_2\handoff.md

TOOL USAGE DIRECTIVE:
DO NOT use `run_command` to create, touch, or edit files. Always use `write_to_file` and `replace_file_content` directly.
Use `run_command` ONLY to run verification (`python -m py_compile ...` or test runs).

MANDATORY INTEGRITY WARNING:
DO NOT CHEAT. All implementations must be genuine. DO NOT hardcode test results, create dummy/facade implementations, or circumvent the intended task. A teamwork_preview_auditor will independently verify your work. Integrity violations WILL be detected and your work WILL be rejected.

WRITE OWNERSHIP:
You exclusively own and may edit ONLY these two files:
- `backend/api.py`
- `backend/test_api.py`
DO NOT modify any other backend files or any frontend files.

OBJECTIVE & IMPLEMENTATION TASKS:
1. `backend/api.py`:
   - In `RawIndicatorsResponse` (around line 94): Add `pattern_candles: list[dict] | None = None` so Pydantic v2 includes the candlestick data generated by `analyze_stock()` in the `/analisis/{kode}` JSON response.
   - In `RecoveryResponse` (around line 300): Change `drop_pct: float` to `drop_pct: float | None = None` so that tickers with fewer than `config.RECOVERY_MIN_BARS` (60 bars) do not trigger a FastAPI `ResponseValidationError` (unhandled 500).
   - In `GET /history/{kode}`: Wrap `fetch_trading_info` in `try...except (YahooClientError, IdxTradingError) as e:` and raise `HTTPException(status_code=502, detail=f"Gagal ambil data historis {kode}: {e}")` instead of letting it raise an unhandled 500.
   - Add calendar date validation helper: Whenever a query date parameter is provided, validate with `date.fromisoformat(target_date)` inside a try-except block, raising `HTTPException(status_code=400, detail="Format tanggal tidak valid")` if a `ValueError` occurs (e.g. for `2026-99-99` or `2026-02-30`).
   - In `GET /readytofly` and `GET /gorengan`: Add defensive try-except handling around cache reads so that empty, missing, or corrupted cache files return HTTP 404 or a clean empty response rather than an unhandled 500.
2. `backend/test_api.py`:
   - Modernize the test suite into structured pytest-compatible test functions (`test_*`).
   - Provide complete test coverage for all endpoints required in acceptance criteria:
     - `GET /market-status`: Status code 200, contains `is_open`.
     - `GET /gainers`: Handles date query, returns 200 or 404 cleanly.
     - `GET /analisis/{kode}`: Status 200 on valid ticker, 404 on unknown ticker, 422 on invalid parameter (e.g. capital <= 0), confirms `pattern_candles` is present in response.
     - `GET /history/{kode}`: Status 200 on valid ticker, 404 on unknown ticker, 502 on upstream network failure.
     - `GET /recovery/{kode}`: Status 200 on valid ticker, 404 on unknown ticker, returns cleanly on short history without 500 error.
     - `GET /readytofly` and `GET /gorengan`: Handles query parameters, returns 200 or 404 without crashing.
     - Boundary validation: Invalid dates (e.g. `?date=2026-99-99`) return 400 or 422.
   - Use standard `TestClient(app)` from `fastapi.testclient`. Use mocks or monkeypatch where appropriate for external network calls so tests run reliably, fast, and deterministically offline.

VERIFICATION:
- Compile backend files: `python -m py_compile backend/api.py backend/test_api.py`
- Run test suite: `pytest backend/test_api.py -v` (or `python backend/test_api.py` if pytest is not available).
- Ensure all tests pass with exit code 0.
- Document all changes and verification outputs in `c:\CodeKuliah\SwingPredictionBot\.agents\worker_m2\handoff.md`.
- Send completion message to parent when done.
